import 'package:flutter/material.dart';

/// Application-wide color palette for SDS Remote.
///
/// Use these constants instead of hardcoded `Color(...)` values to
/// keep the visual identity consistent and easy to restyle.
class AppColors {
  const AppColors._();

  /// Main scaffold background (very dark blue-grey).
  static const scaffoldBackground = Color(0xFF0D1117);

  /// Panel / card background (dark navy).
  static const panelBackground = Color(0xFF0A192F);

  /// Panel borders and dividers.
  static const border = Color(0xFF475569);

  /// Status bar background (near-black).
  static const statusBar = Color(0xFF151515);
}
